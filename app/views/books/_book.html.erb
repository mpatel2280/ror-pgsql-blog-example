<div id="<%= dom_id book %>" class="text-3xl font-bold underline">
  <p>
    <strong>Title:</strong>
    <%= title_uppercase(book.title) %>
  </p>

  <p>
    <strong>Content:</strong>
    <%= book.content %>
  </p>

</div>
